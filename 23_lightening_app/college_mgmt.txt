1. Marks must be between 0 and 100
OR(
    Marks__c < 0,
    Marks__c > 100
)

2. Roll Number must be greater than 0
Roll_Number__c <= 0

3. Student Name cannot be blank
ISBLANK(Name)

4. Email must contain "@"

(If Email field is TEXT type)

NOT(CONTAINS(Email__c, "@"))

1. Faculty Name must be at least 3 characters
LEN(Name) < 3

2. Faculty ID must be greater than 0
Faculty_ID__c <= 0

3. Salary between 10,000 and 500,000
OR(
    Salary__c < 10000,
    Salary__c > 500000
)

4. Department must be selected
ISBLANK(Department__c)

5. Joining Date cannot be future date
Joining_Date__c > TODAY()

6. Email must contain "@"
NOT(CONTAINS(Email__c, "@"))